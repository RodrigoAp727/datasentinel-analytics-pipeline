required_columns:
  - id
  - value

null_threshold: 0.2
duplicate_ratio_threshold: 0.0

expected_types:
  id: int
  value: float
  category: string

outlier_detection:
  method: iqr
  max_outlier_ratio: 0.05
  zscore_threshold: 3.0
  columns:
    - value
